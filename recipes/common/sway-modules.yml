modules:
    - type: dnf
      install:
        install-weak-deps: false
        packages:
          - kitty
          - swaylock
          - swayidle

    - type: files
      files:
        - source: system/sway
          destination: /